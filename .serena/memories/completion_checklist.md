# Task completion checklist

- Run pnpm lint and pnpm typecheck before submitting changes.
- Run pnpm format if formatting changes are needed.
- Smoke-test with pnpm dev; restart the app if main/preload code changed.
- In PRs, note OS tested and include screenshots for UI changes.
